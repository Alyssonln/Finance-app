import { styles } from '@/styles/globals';
import { Text, View } from 'react-native';

const ViewProfileScreen = () => {
   return (
      <View style={styles.container}>
         <Text style={styles.sectionTitle}>Ver perfil</Text>
      </View>
   );
};

export default ViewProfileScreen;
